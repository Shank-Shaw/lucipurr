<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <property name="LEVEL" value="INFO"/>
    <property name="LOGS" value="C:\Users\Shashank Shekhar\Documents\Logs\lucipurr"/>
    <property name="LOGS_ARCHIVE" value="${LOGS}/archive"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd} | %d{HH:mm:ss.SSS} | %X{traceId:-} | %X{spanId:-} | %-35.35thread | %5p |
                %-35.35logger{35} | %m%n
            </pattern>
        </encoder>
    </appender>

    <appender name="FILE-ROLLING" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS}/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOGS_ARCHIVE}/app.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- each archived file, size max 20MB -->
            <maxFileSize>20MB</maxFileSize>
            <!-- total size of all archive files, if total size > 5GB, it will delete old archived file -->
            <totalSizeCap>5GB</totalSizeCap>
            <!-- 30 days to keep -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>
                %d{yyyy-MM-dd} | %d{HH:mm:ss.SSS} | %X{traceId:-} | %X{spanId:-} | %-20.20thread | %5p |
                %-25.25logger{25} | %m%n
            </pattern>
        </encoder>
    </appender>

    <root level="${LEVEL}">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE-ROLLING"/>
    </root>
</configuration>